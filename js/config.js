const config = {
    API_URL: 'https://laughable-news-production.up.railway.app',
    API_CONFIG: {
        credentials: 'include',
        headers: {
            'Content-Type': 'application/json'
        }
    }
};